kind: pipeline
type: kubernetes
name: k3s-start

steps:
  - name: node install
    image: node:alpine
    commands:
      - yarn --registry=https://registry.npm.taobao.org
  - name: node build
    image: node:alpine
    commands:
      - yarn build
